{
  "name": "@aieos/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "check:runtime": "pnpm build && node dist/check-runtime.js"
  },
  "dependencies": {
    "@aieos/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5"
  }
}
